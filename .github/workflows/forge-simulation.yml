name: Forge Simulation

on:
  workflow_dispatch:
    inputs:
      games:
        description: "Number of games to run"
        required: false
        default: "20"
      seed:
        description: "Base random seed"
        required: false
        default: "2026082501"

permissions:
  contents: read

jobs:
  simulate:
    name: Forge simulation
    runs-on: ubuntu-latest
    timeout-minutes: 60

    env:
      FORGE_VERSION: "2.0.14"
      JAVA_TOOL_OPTIONS: "-Djava.awt.headless=false"
      GAMES: ${{ inputs.games || '20' }}
      BASE_SEED: ${{ inputs.seed || '2026082501' }}

    steps:

      # ------------------------------------------------------------
      # Checkout repository
      # ------------------------------------------------------------
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # ------------------------------------------------------------
      # Java 17
      # ------------------------------------------------------------
      - name: Set up Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"
  

      # ------------------------------------------------------------
      # Python
      # ------------------------------------------------------------
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      # ------------------------------------------------------------
      # Install virtual X display
      #
      # Forge's WeSayTheeNayTest initializes GuiDesktop, which
      # requires an X11 display. Xvfb provides that display in CI.
      # ------------------------------------------------------------
      - name: Install Xvfb
        run: |
          sudo apt-get update
          sudo apt-get install -y xvfb
          Xvfb -version

      # ------------------------------------------------------------
      # Inspect repository
      # ------------------------------------------------------------
      - name: Inspect repository
        shell: bash
        run: |
          set -euxo pipefail

          echo "Repository:"
          pwd

          echo
          echo "Top-level files:"
          find . -maxdepth 2 -type f | sort | head -300

          echo
          echo "Forge build scripts:"
          find engine -maxdepth 4 -type f -print 2>/dev/null || true

          echo
          echo "Workflow:"
          find .github -maxdepth 3 -type f -print 2>/dev/null || true

      # ------------------------------------------------------------
      # Validate build script
      # ------------------------------------------------------------
      - name: Check Forge build script
        shell: bash
        run: |
          set -euxo pipefail

          test -f engine/forge/build_patched_2_0_14.sh

          chmod +x engine/forge/build_patched_2_0_14.sh

          echo "Build script found:"
          ls -lh engine/forge/build_patched_2_0_14.sh

      # ------------------------------------------------------------
      # Download Forge 2.0.14 source
      # ------------------------------------------------------------
      - name: Download Forge 2.0.14 source
        shell: bash
        run: |
          set -euxo pipefail

          rm -rf forge-2.0.14-source

          git clone --depth 1 \
            --branch forge-2.0.14 \
            https://github.com/Card-Forge/forge.git \
            forge-2.0.14-source

          cd forge-2.0.14-source

          echo "Forge revision:"
          git rev-parse HEAD

          echo "Forge tags:"
          git tag --points-at HEAD || true

      # ------------------------------------------------------------
      # Create Maven local settings
      # ------------------------------------------------------------
      - name: Create Maven local settings
        shell: bash
        run: |
          set -euxo pipefail

          mkdir -p forge-2.0.14-source/.mvn

          cat > forge-2.0.14-source/.mvn/local-settings.xml <<'EOF'
          <?xml version="1.0" encoding="UTF-8"?>
          <settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
                    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                    xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0
                    https://maven.apache.org/xsd/settings-1.0.0.xsd">
            <interactiveMode>false</interactiveMode>
          </settings>
          EOF

          test -f forge-2.0.14-source/.mvn/local-settings.xml

      # ------------------------------------------------------------
      # Build patched Forge under Xvfb
      #
      # IMPORTANT:
      # The entire build script is wrapped in xvfb-run.
      # Therefore WeSayTheeNayTest gets a valid DISPLAY.
      # ------------------------------------------------------------
      - name: Build patched Forge 2.0.14
        shell: bash
        run: |
          set -euxo pipefail

          chmod +x engine/forge/build_patched_2_0_14.sh

          xvfb-run \
            --auto-servernum \
            --server-args="-screen 0 1920x1080x24" \
            ./engine/forge/build_patched_2_0_14.sh \
              "$GITHUB_WORKSPACE/forge-2.0.14-source"

      # ------------------------------------------------------------
      # Locate Forge runtime
      # ------------------------------------------------------------
      - name: Locate Forge runtime
        shell: bash
        run: |
          set -euxo pipefail

          echo "Searching for Forge runtime..."

          find forge-2.0.14-source \
            -type f \
            \( \
              -name "*.jar" \
              -o -name "forge*.jar" \
            \) \
            -print | sort | head -200

      # ------------------------------------------------------------
      # Run Forge regression tests explicitly under Xvfb
      # ------------------------------------------------------------
      - name: Run Forge regression tests
        shell: bash
        run: |
          set -euxo pipefail

          cd forge-2.0.14-source

          xvfb-run \
            --auto-servernum \
            --server-args="-screen 0 1920x1080x24" \
            mvn -B \
              -Djava.awt.headless=false \
              -DskipTests=false \
              test

      # ------------------------------------------------------------
      # Verify the specific card test
      # ------------------------------------------------------------
      - name: Verify We Say Thee Nay test
        shell: bash
        run: |
          set -euxo pipefail

          cd forge-2.0.14-source

          echo "Searching for WeSayTheeNayTest..."
          find . -type f \
            \( \
              -name "*WeSayTheeNay*" \
              -o -name "*We_Say_Thee_Nay*" \
            \) \
            -print

          echo
          echo "Surefire reports:"
          find . \
            -path "*/target/surefire-reports/*" \
            -type f \
            -print | sort | head -200

      # ------------------------------------------------------------
      # Run the Izzet Spellementals experiment
      # ------------------------------------------------------------
      - name: Run Izzet Spellementals experiment
        shell: bash
        run: |
          set -euxo pipefail

          echo "Games: $GAMES"
          echo "Base seed: $BASE_SEED"

          if [ -f scripts/run_experiment.py ]; then
            python scripts/run_experiment.py \
              --games "$GAMES" \
              --seed "$BASE_SEED"

          elif [ -f engine/simulation/run_experiment.py ]; then
            python engine/simulation/run_experiment.py \
              --games "$GAMES" \
              --seed "$BASE_SEED"

          elif [ -f simulation/run_experiment.py ]; then
            python simulation/run_experiment.py \
              --games "$GAMES" \
              --seed "$BASE_SEED"

          else
            echo "No experiment runner found."
            echo "Repository Python files:"
            find . -type f -name "*.py" | sort
            exit 1
          fi

      # ------------------------------------------------------------
      # Display experiment results
      # ------------------------------------------------------------
      - name: Display experiment results
        if: always()
        shell: bash
        run: |
          set +e

          echo "========================================"
          echo "Simulation result files"
          echo "========================================"

          find . \
            -type f \
            \( \
              -name "*.json" \
              -o -name "*.jsonl" \
              -o -name "*.csv" \
              -o -name "*.md" \
              -o -name "*.log" \
            \) \
            -not -path "./.git/*" \
            -print | sort

          echo
          echo "========================================"
          echo "Recent result files"
          echo "========================================"

          find . \
            -type f \
            \( \
              -name "*.json" \
              -o -name "*.jsonl" \
              -o -name "*.csv" \
              -o -name "*.log" \
            \) \
            -not -path "./.git/*" \
            -print \
            -exec sh -c 'echo "----- $1 -----"; tail -100 "$1"' _ {} \;

      # ------------------------------------------------------------
      # Upload all simulation/build diagnostics
      # ------------------------------------------------------------
      - name: Upload simulation results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: forge-simulation-results
          path: |
            forge-2.0.14-source/**/target/surefire-reports/**
            forge-2.0.14-source/**/target/*.log
            forge-2.0.14-source/**/target/*.txt
            results/**
            output/**
            artifacts/**
            logs/**
            reports/**
          if-no-files-found: warn
          retention-days: 14
